#include <stdio.h>
#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void);
{
    for (i = 1 ; i <= 10 ; i++)
    {
        for (char k = 'a';  k =< 'z' ; k++)
        {
            _putchar(a)
            _putchar("\n")
        }
    }
return (0);
}
