#include <stdio.h>
#include "main.h"
/**
 * _islower - writes the character c to stdout
 * @c: The character to print
 *
 * Return: On success 1
 */
void print_to_98(int n)
{
	int i; 
        if (n <= 98)
        {
		for (i = n ; i <= 98 ; i++)
			printf("%d ," ,i);
			if (i = 98)
				printf("%d",i);
        }
        else if (n >= 98)
        {
		for (i = n ; i >= 98; i--)
			printf("%d ," ,i);
			if ("%d",
        }
	else 
		printf("%d" ,n);
	printf("\n");
}
