#include <stdio.h>
#include <math.h>
#include "main.h"
/**
 * _islower - writes the character c to stdout
 * @c: The character to print
 *
 * Return: On success 1
 */
        int print_last_digit(int n)
{
	int  l;
	l = n % 10;
	_putchar(l);
	return(0);
}
