#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 *  _abs - writes the character c to stdout
 * @c: The character to print
 *
 * Return: On success 1
 */
int _abs(int n);
{
	n = abs(n);
	_putchar(n);
return (0);
}
