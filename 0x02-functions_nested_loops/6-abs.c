#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * _abs - writes the character n to stdout
 * @n: The character to print
 *
 * Return: On success 1
 */
int _abs(int n)
	{
	n = abs(n);
	return (n);
	}
